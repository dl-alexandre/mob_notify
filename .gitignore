/_build
/deps
erl_crash.dump
*.beam
.DS_Store

# Fork consumers pin this Git tree rather than the signed Hex artifact, so the
# reviewed signature must travel with the pinned source revision.
!priv/mob_plugin.sig
